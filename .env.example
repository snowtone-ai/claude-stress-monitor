# claude-stress-monitor — .env.example
#
# このシステムは外部APIを使用しません。
# 環境変数の設定は不要です。
#
# システムが使用するファイルパス（変更不可）:
#   ~/.claude/stress-state.json  (フックが書き込み、拡張が読み取り)
#   ~/.claude/settings.json      (グローバルフック登録先)
#   ~/.claude/hooks/stress-signal.mjs (フック本体)
